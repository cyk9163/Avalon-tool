# 更新记录

## 1.19.0 · 自己选号码，被占了可以申请互换 · 2026-09-25

- 进房间后，点圆桌上的空位就是坐那个号码。线下围坐时，号码要和实际位置一致。
- 号码已经有人时，点那个号向对方申请互换。对方同意后两人对调号码，并且都要重新准备。对方也可以拒绝。还没入座时，先坐一个空位，再申请互换。发身份之后不能再换号。

## 1.18.0 · 头像标记与可选轮流发言 · 2026-09-25

- 标记其他玩家：点圆桌上的头像，弹出菜单选阵营或身份。选中的标记是一个字，显示在头像右上角。选队时点头像仍是上车，点右上角才打开标记。笔记里的文字备注还在，阵营和身份不再在笔记里选。
- 建房时可以勾选「轮流发言」。不勾选（默认）是线下讨论：队长亮车，大家说完直接表决，不用每人点「我说完了」。勾选后仍是原来的发言顺序和计时。已经建好的旧房间保持轮流发言。

## 1.17.0 · 本地一个人测整桌 · 2026-09-25

一个人可以在本地点完所有座位。界面在没有手动选过语言时默认中文。

- 没有点过「EN / 中文」时，页面用中文，不再跟着浏览器语言走。点过英文的设备仍保持英文。
- 本地打开 http://127.0.0.1:5173/solo ，选人数和板子，点「摆好一桌」。页面会排出 5–10 个窗口，每个窗口是一个独立座位（小明、阿花、老王……）。
- 每个窗口都是正式的对局页面。可以在不同窗口里看身份、准备、组队、投票、出任务牌、湖中仙女、刺杀、结局回放和重开。
- 窗口之间用请求头 `X-Avalon-Solo` 区分设备。这个头只在本机域名上生效；正式域名会忽略它，`/api/solo` 也会返回 404。线上对局和权限没有改。

## 1.16.0 · 发布、断线与运营计数 · 2026-09-23

让网站更稳，不改游戏规则。

- 推到 `main` 后，如果仓库配了 `CLOUDFLARE_API_TOKEN`，会自动迁移并部署 staging。正式环境要在 Actions 里手动运行 Deploy，并勾上「真机清单已做完」，见 `RELEASE.md`。
- 断网或超时再按投票时，页面会写「上一动作没送出，请再按一次」。服务器已经收下的操作，重连时会写「刚才的操作已经在服务器上」。已经成功的动作不会因为再按一次而执行两遍。
- 管理后台新增三个汇总数字：已开局、已结束或留有战绩、同房重开过。仍然看不到房间码、昵称、身份或投票。操作日志多了一个 `outcome` 字段（ok / rejected / unavailable），方便按错误编号检索。

## 1.15.0 · 依赖自动更新 · 2026-09-23

不改功能。

- 新增 `.github/dependabot.yml`：npm 每周检查一次，同时最多 5 个 PR，并分成 cloudflare、vinext、react-next、test-tools 四组；GitHub Actions 每月检查一次，最多 3 个 PR。vinext 单独成组，因为它仍是测试版，合并前要看变更说明，等 CI 全绿，并先在 staging 验证再上正式。
- `npm ci` 时可能出现 allow-scripts 警告，涉及 esbuild、unrs-resolver、workerd 的安装脚本。它们只下载或校验本机二进制，供 Vite、ESLint 和 Wrangler 使用。结论是保持默认，不运行 `npm approve-scripts`，也不把批准记录提交进仓库。警告不会让安装失败。如果以后的 npm 改成默认不执行这些脚本，再只批准这三家，不要一次批准全部依赖。
- 合并升级 PR：在 GitHub 打开 Dependabot 的 PR，等自动检查全绿；vinext 那一组先读变更说明，部署 staging 确认后再合并。在 PR 页面点 Merge 即可，不要强制推送。

## 1.14.0 · 拆分大文件 · 2026-09-23

只拆文件，不改规则、接口、文案或样式。

- `lib/game.ts`（原先约 1155 行）拆成 `lib/game/model.ts`（411 行，角色与板子）、`lib/game/view.ts`（124 行，视图投影）、`lib/game/play.ts`（329 行，对局动作）、`lib/game/manage.ts`（307 行，入座、移交、移出和恢复）。`lib/game.ts` 仍是统一出口，原来的导入路径不用改。
- `app/page.tsx`（原先约 363 行）现在约 284 行。会话请求放到 `lib/room-client.ts`，实时同步和轮询放到 `lib/use-room-sync.ts`，圆桌座位放到 `components/seat-table.tsx`，使用说明弹窗放到 `components/help-dialog.tsx`。手机大厅的 DOM 层级没有改。

## 1.13.0 · 锁屏时收到「轮到你」 · 2026-09-23

- 房间里可以主动开启锁屏提醒。浏览器不会一进房间就弹权限；点「开启锁屏提醒」才请求通知，也可以随时关闭。安卓 Chrome 可以直接用；iPhone 需要 iOS 16.4 以上并且先添加到主屏幕。
- 轮到你发言、选队、投票、出牌、使用湖中仙女或刺杀时，手机会收到一条通知，例如「轮到你投票 · 房间 123456」。通知里没有身份、昵称或投票。同一个人还停在同一个提示上时不会再发。
- 推送地址和密钥存在单独的表里，房间码只存摘要，跟房间一起在 24 小时后由定时清理删除。推送服务退回失效地址时会删掉这条订阅。签名用 VAPID，内容用 aes128gcm，只依赖 Workers 自带的 WebCrypto。私钥只放在 `work/` 和 Cloudflare Secret `VAPID_PRIVATE_KEY`，公钥在 `lib/vapid-public.ts`。没配置私钥时对局照常进行，只是不发通知。
- 免费额度：一次状态变化最多给当前新轮到的几个人各发一条（一桌最多 10 人）。Workers 免费方案每天约 10 万次子请求；按一晚 50 局、每局 30 次变化、平均 5 条通知估算，大约 7500 次，远低于免费额度。
- 隐私说明已补充。新增「谁新轮到」、订阅校验、过期清理和本地假推送端点的测试；浏览器测试只检查开启按钮和提示，真实推送要在真机上验。

## 1.12.0 · 复盘回放、结局高光与本机战绩 · 2026-09-23

- 结局回放：本局成员在结局页可以逐步回放。每一步按发生顺序展示一车（队长、提交的队伍、每人的表决、通过或否决）、一次任务（队员、失败张数，以及结局后才公开的出牌），以及你自己经手的湖中仙女交接（只显示谁把令牌交给谁，查验结果仍然只有查验者自己看得到）。圆桌同步显示那一步的队长、车上的人和已表决标记，并带上当时的比分和任务轨道。旁观者没有回放。数据全部来自现有视图，没有改服务端。
- 结局高光：根据已经公开的身份和表决，自动挑出最多 4 条，例如坏人上了所有失败的任务、梅林的反对有几次押中了后来失败的车、派西维尔从某一次组队起只赞成有梅林的队伍、刺客有没有找到梅林、连续否决最多的一段。没有合适的条目就不显示。同一批文字也写进复盘文字和复盘长图。
- 本机个人战绩：对局结束时，这台设备记下日期、人数、板子、我的角色、最终阵营、胜负，以及这局有没有刺中梅林。只存在 localStorage，不上传；同一房间的同一局只记一次，保留最近 200 局。首页、规则页和隐私页旁新增「我的战绩」（`/me`）：总胜率、各阵营和各角色胜率、当梅林被刺中的次数、最近 10 局，以及「清除本机战绩」。隐私说明已补充。
- 新增回放顺序、任务牌只在结局后出现、旁观者没有回放、每条高光规则、本机战绩去重与统计的单元测试；浏览器测试打完一局后逐步回放，并确认战绩写在本机；无障碍检查覆盖 `/me`（含浅色主题）。

## 1.11.0 · 新手提示与浅色主题 · 2026-09-23

- 新手提示：房间顶部显示一行「现在该做什么」，按你自己的处境变化：入座、准备、按住看身份、轮到你发言、当队长时如何亮车和发起表决、怎么投票、在车上怎么出牌、湖中仙女、刺杀，以及结局后的复盘。点 × 关闭后这台设备不再显示，可以在右上角「?」帮助里重新打开。提示只根据你自己能看到的信息生成，不会透露别人的身份。
- 浅色主题：右上角新增太阳／月亮按钮，在深色和浅色之间切换，适合白天、户外或灯光很亮的地方。选择保存在 Cookie `avalon_theme` 里（只有 light／dark 两个值），服务端直接按主题渲染，不会闪烁；首页、房间、规则、隐私、管理后台和大屏都支持。
- 浅色主题由 `scripts/theme-light.mjs`（`npm run theme:build`）从现有样式自动生成 `app/theme-light.css`：在 OKLab 色彩空间里翻转每个颜色的明暗、保留色相，强调色（金、绿、红）变成浅底上易读的深色版本，阴影保持深色；单元测试会检查生成文件与样式同步。首页本来就是浅色的建房卡片保持原样。
- 无障碍检查新增浅色主题（含颜色对比度），并修正了检查发现的一处发言标题对比度不足。
- 新增新手提示、主题配色和生成文件同步的单元测试，以及主题切换、提示关闭的浏览器测试。
- `HANDOFF.md` 新增「待办计划：v1.12–v1.15」（复盘回放与结局高光与本机战绩、锁屏推送、拆分大文件、依赖自动更新），本阶段后暂停开发。

## 1.10.0 · 大屏模式、无障碍与线上监控 · 2026-09-23

- 大屏模式：房主或任何玩家在「邀请入座」里点「打开大屏模式」，在桌子中间的平板或电脑上显示对局的公开信息：大号圆桌（队长、亮车、谁在发言、谁已投票）、比分和任务轨道、当前阶段、发言顺序与计时、表决揭晓动画和投票矩阵。大屏没有任何按钮，也不占座位；服务端对大屏只返回公开视图，**不含任何人的身份、湖中仙女结果或出牌记录，结局后也一样**（身份和出牌只在各自手机上揭晓）；即使在某位玩家自己的设备上打开，大屏也不会带上这位玩家的身份。需要邀请口令才能看到对局，打开后口令会从地址栏移除。
- 色盲友好：「本局阵容」的角色标签、结局的身份揭晓都在颜色之外加了阵营图标（盾牌＝正义，双剑＝邪恶），角色按钮的读屏名称也带上阵营；高对比度（强制颜色）模式下，车上金框、揭晓卡片和当前发言人仍然可以分辨。
- 读屏：轮到你操作时会有一条读屏播报；表决和任务揭晓会读出一句完整结果（例如「任务 1 · 第 1 车表决：通过，4 赞成 · 1 反对」）；手机对局中收起的大标题改为只对读屏保留，页面始终有一级标题。
- 自动无障碍检查：浏览器测试新增 axe-core 检查（WCAG 2.1 A/AA），覆盖首页、规则页、隐私页、选队、表决和大屏，出现严重问题即失败；本次修复了它发现的大屏连接状态标记的属性问题。
- 线上监控：新增每小时一次的 GitHub Actions 只读检查（健康与数据库、安全头、规则／隐私页、管理接口上锁、实时通道），失败时 GitHub 会发邮件给仓库所有者；也可以在 Actions 页面手动运行。`scripts/smoke.mjs` 新增 `AVALON_SMOKE_ANY_VERSION=1`，监控时不要求版本号与仓库一致。
- 隐私说明补充复盘图片、同房战绩和大屏模式的说明。
- 新增大屏视图权限的单元测试，以及大屏和无障碍的浏览器测试。

## 1.9.0 · 同房战绩、复盘长图与角色说明卡 · 2026-09-23

- 同房战绩：同一个房间连续玩多局时，结局页（第二局起）和下一局的大厅里显示「同房战绩」：总局数与正义／邪恶各胜几局，每位玩家的胜场、当正义和当邪恶时的胜场／场数、当梅林被刺中的次数，以及每一局的结果。每局结束时服务端记下一条结局记录（局数、胜方、原因、每位玩家的角色和最终阵营，兰斯洛特按最终阵营），同房重开后保留，房间过期时一起删除。只统计、也只向你显示你参与过的对局；中途加入的新玩家看不到之前对局的身份。
- 复盘长图：结局页新增「保存图片」，在手机上直接生成一张竖版长图（结果、全部身份、任务轨道与每次任务谁出了哪张牌、组队表决矩阵），手机上弹出系统分享面板（可发到微信或存到相册），电脑上下载 PNG。图片在本机生成，不经过服务器。
- 角色说明卡：大厅和建房时的「本局阵容」角色标签、结局的身份揭晓里，点角色名弹出说明卡：阵营、角色说明、能看到谁、谁能看到他、能出什么任务牌（按本站规则）。
- 修复手机大厅排版：v1.6 加入的「本局规则」卡和「本局阵容」卡被放在同一行，手机上大厅被挤成两列，左侧只剩很窄的一条。现在每张卡片单独一行，并在浏览器测试中检查大厅卡片都是整屏宽度。
- 新增结局记录（记录一次、跨局保留、新玩家看不到旧对局）、战绩统计、角色说明完整性的单元测试，以及角色说明卡和大厅排版的浏览器测试。

## 1.8.0 · 发言顺序、轮到你提醒与揭晓动画 · 2026-09-23

- 发言顺序与计时：选队阶段显示本车的发言顺序（队长先说，再按座位号顺时针），圆桌上正在发言的人带麦克风标记。发言的人说完点「我说完了」；队长或房主可以「下一位」跳过不在的人、「再轮一遍」，并设置每人的软计时（不计时／60／90／120／180 秒，默认 90 秒，之后的每一车沿用）。所有手机按服务器时间显示同一个倒计时，超时后变红并在发言人的安卓手机上轻震一下，不会强制打断。（服务端新增 `speech` 动作，只在选队阶段有效；发言状态公开，不含任何隐藏信息。）
- 轮到你操作时的提醒：轮到你发言、选队、投票、出任务牌、使用湖中仙女或刺杀时，浏览器标签标题前出现「● 轮到你…」，安卓手机轻震一下，已添加到主屏幕的应用显示角标；手机上的分区导航「对局」旁出现一个红点。操作完成后自动清除。
- 揭晓动画：组队表决全员提交、或任务牌全部提交后，每台正在看的手机弹出「3、2、1」倒数，逐张翻开每个人的赞成／反对（任务牌只翻张数，顺序与出牌人无关），再显示通过／否决或任务成功／失败。点任意处或「跳过」关闭，约 5 秒后自动关闭；开启「减少动态效果」时直接显示结果；之后才打开房间的人不会重放旧结果。
- 新增发言顺序、权限、计时沿用和「轮到谁」的单元测试，以及发言轮转、标签标题提醒和揭晓动画的 WebKit／Chromium 浏览器测试。

## 1.7.0 · 投票矩阵、玩家统计与手机浏览器自动化测试 · 2026-09-23

- 「对局记录」的组队表决改为投票矩阵：每一车一行、每个座位一列，✓ 为赞成、✗ 为反对（用图标区分，不只靠颜色），金框表示这个人在车上，皇冠表示队长，最右列为通过／否决。10 人局在 390px 宽的手机上也不需要左右滑动。
- 矩阵下方新增「玩家统计」：每位玩家当队长、被选上车、出任务的次数和赞成率，只用公开的表决与任务记录计算，对局中随时可看。
- 复制／下载的复盘文字新增【玩家统计】一段，中英文都支持。
- 新增手机浏览器自动化测试（Playwright）：用 WebKit 引擎模拟 iPhone 13、Chromium 模拟 Pixel 7，分别跑完「队长在圆桌上选人 → 亮车 → 改车 → 发起表决 → 另一台手机实时看到并投票」、按住查看身份与切走时自动隐藏、断网显示「重连中」后自动恢复，以及首页／规则页／隐私页和对局页没有横向滚动。本地运行 `npm run test:e2e`，CI 新增对应任务，失败时上传测试报告。
- 新增玩家统计与复盘统计的单元测试。

## 1.6.0 · 圆桌主视图、亮车与本局规则 · 2026-09-23

- 圆桌成为对局主视图：对局区直接显示圆桌，队长戴皇冠、车上的人金色高亮、表决阶段显示谁已投票、湖中仙女持有者和被公开的揭露者有标记，你的推理标记显示为座位外圈颜色和名字下方的标签；圆桌中央显示当前任务、需要人数和第几车。
- 队长直接在圆桌上点选队员，取代原来的选人列表。
- 新增「亮车」：队长选好人后先亮车，全桌的圆桌和「队长亮车」卡片会同步显示；所有人发言后可以改车（次数不限）或撤回，最后点「发起表决」才进入全员表决，发起后这一车不能再改。换下一任队长时亮车自动清空。（服务端新增 `draft` 动作，仅本轮队长在选队阶段可用，人数不超过本次任务人数。）
- 队长顺序与第五车警示：选队和表决阶段显示本任务剩余的每一车由几号当队长，第五车标红；当前是第五车时醒目提示「本车若被否决，邪恶直接获胜」。
- 本局规则卡：大厅里显示一张只包含本局适用规则的卡片（随机身份与队长、过半通过、第四任务两张失败、坏人互认、刺客随时出刀、兰斯洛特／湖中仙女／疯子／野蛮人／揭露者等按板子出现、结局公开出牌）；对局中通过「本局规则」按钮随时查看。
- 手机上当前的主要操作（选队、表决、提交任务牌）改为固定在屏幕底部的操作栏，不受页面滚动影响。
- 新增亮车的单元测试。

## 1.5.0 · 结局公开出牌记录 · 2026-09-23

- 按用户要求调整保密规则：对局进行中仍然只公布每次任务的成功／失败张数，服务端不会把出牌人发给任何设备；**游戏结束后**，结局数据中新增 `questCards`，向本局成员公开每次任务谁出了成功、谁出了失败（旁观者看不到，与身份揭晓的范围相同）。
- 结局页的「对局记录」在每次任务下列出每位队员的出牌（成功为绿色、失败为红色）；「复制复盘文字」和「下载 .txt」在每次任务下增加「出牌：1 号 小明 成功、2 号 阿花 失败」一行，中英文都支持。
- 游戏内、规则教学页、隐私说明、导出提示和 `AGENTS.md`／`HANDOFF.md` 中的相关说明同步改为「对局中保密，结局后公开」。
- 新增测试：对局中每个阶段 `questCards` 均为空、结局后每位成员看到的出牌与实际一致、旁观者拿不到对局数据、复盘文字包含出牌行。

## 1.4.0 · 手机交互优化 · 2026-09-23

只改界面，不改任何规则、权限或数据逻辑。参考主流手机端设计的做法：主要操作放在拇指区、分区导航固定在顶部、44px 点按区域、适配刘海屏安全区。

- 对局中（手机）新增固定在顶部的分区导航「对局 · 我的身份 · 笔记 · 记录」：一点即跳到对应区域，折叠的区域会自动展开，并高亮当前所在的区域；导航右侧的小圆点显示连接状态（绿色为实时，断线时显示「重连中」），替代原来单独一行的进度条。
- 当前的主要操作固定在屏幕底部：「提交队伍」「赞成 / 反对」「私密提交任务票」、湖中仙女查验和刺杀确认，不用再滑动去找按钮。
- 对局中收起房间的大标题和标语，只保留人数、板子、局数和房间码；游戏面板的英文小标题在手机上隐藏。实测 390×844 手机上，队长的「选出任务队伍」和「提交队伍」都在第一屏。
- 「我的推理笔记」「我的身份与圆桌座位」「房间管理」统一为同样的间距、圆角和标题高度，不再挤在一起。
- 触屏设备上：按钮有按压反馈，阵营标记、下拉框、删除模板、语言切换、文字按钮的点按区域不小于 44px（删除模板为 40px）；去掉点按时的灰色高亮。
- 适配刘海屏和底部横条（viewport-fit=cover + 安全区内边距）；尊重系统的「减少动态效果」设置。
- 桌面端布局不变。

## 1.3.0 · 兰斯洛特阵营转换 · 2026-09-23

- 兰斯洛特改为官方阵营转换规则（变体 2）：开局时从 5 张忠诚牌（2 张「转换」、3 张「不变」）中抽出 3 张，分别对应第 3、4、5 轮，并向全桌公开，所有人都知道哪一轮会转换。翻到「转换」的那一轮起，两位兰斯洛特互换阵营；再遇到一张「转换」就换回来。
- 出牌按当前阵营强制：属于正义的兰斯洛特只能出成功，属于邪恶的兰斯洛特只能出失败，服务端校验。湖中仙女查验兰斯洛特时看到的是他当前的阵营；兰斯洛特自己的身份卡显示当前阵营。
- 两位兰斯洛特不再互相认识（官方变体 1、2 的做法）。按本站规则，邪恶兰斯洛特仍和其他坏人互相知道具体角色，梅林仍能看到他是坏人；这些都是开局时获得的信息，阵营转换后不变。
- 对局界面新增公开的「兰斯洛特忠诚牌」轨道，写明当前两人的出牌规则；结局揭晓和导出的复盘按兰斯洛特的最终阵营显示。
- 两位兰斯洛特的角色说明、规则教学页、推荐板子说明和 README 同步更新；新增忠诚牌发放、转换时机、强制出牌、湖中仙女查验和身份阵营的单元测试。
- 说明：官方规则书原文未能直接查阅（BoardGameGeek 的官方规则文件无法访问），以上按多处资料一致的内容实现；与你们桌上的习惯不同时可以再调整。

## 1.2.1 · 发言草稿 · 2026-09-23

- 「我的推理笔记」顶部新增「我的发言准备」文字框（最多 600 字），可以提前写好轮到自己时想说的话。和其他笔记一样只保存在本机、不上传服务器，按房间和局次分开，同房再开换新，可以一键清空；笔记标题会提示「有发言草稿」。
- 隐私说明同步补充；新增草稿的数据清理测试。

## 1.2.0 · 本站固定规则 · 2026-09-23

- 坏人之间互相知道具体角色：除奥伯伦外，每个邪恶玩家的线索里直接显示同伴的角色名（例如「4 号 · 莫甘娜」），不再只显示「邪恶同伴」；邪恶兰斯洛特同样能看到同伴的角色。奥伯伦依旧看不到别人，别人也看不到他。
- 梅林保持原样：只知道谁是坏人（不知道具体角色），看不到莫德雷德；派西维尔看到梅林和莫甘娜两位候选人但分不清。
- 「刺客随时出刀」改为所有房间的固定规则，去掉建房开关和房间选项 `anytimeAssassin`：对局中刺客可以随时出刀一次，刺中梅林邪恶获胜，刺错正义获胜；一直没出刀则在三次任务成功后自动进入最后刺杀。
- 刺客的角色说明、规则教学页（标明这两条是本站规则、与官方不同）、使用说明和英文翻译同步更新。
- 相关测试改为验证新规则：坏人同伴显示具体角色、梅林只看到「已知邪恶」、任何房间都能提前出刀。

## 1.1.0 · 刺客随时出刀与推理笔记 · 2026-09-23

- 新增规则选项「刺客可随时出刀」，建房时默认开启：对局开始后（选队、表决、任务、湖中仙女阶段），刺客可以在任意时刻刺杀一次，刺中梅林邪恶立即获胜，刺错正义立即获胜，因此一局最多一次；如果一直没有出刀，好人三次任务成功后照常自动进入最后刺杀。关闭后按官方规则。旧房间保持原规则。
- 出刀按钮只出现在刺客自己设备上、默认收起的「我的身份与圆桌座位」里，旁人扫一眼屏幕看不出；选目标后还要再确认一次，页面切到后台会关闭并清空选择。服务端只接受本局当前回合、刺客本人、非自己的目标；重试同一请求不会重复结算，更换目标会被拒绝。
- 结局页和导出的复盘会注明「提前出刀」；规则教学页和使用说明补充了这条规则。
- 新增「我的推理笔记」：对局中可以把其他玩家标记为好人、坏人，或本局板子中的某个角色（选角色会自动带上阵营），并为每位玩家写发言备注（最多 300 字）。标记会显示在选队、湖中仙女、刺杀的玩家列表旁。
- 笔记只保存在本机浏览器，不上传服务器、其他玩家看不到；按房间和局次分开保存，同房再开自动换一张新笔记，只保留最近 5 局，可以一键清空。隐私说明已补充。
- 新增随时出刀的单元测试和接口集成测试，以及笔记数据清理的单元测试。

## 1.0.0 · 中英双语与正式版 · 2026-09-23

- 全站支持中文和英文：首页、房间、对局、换设备、房间管理、结局复盘、规则教学、隐私说明、管理后台和断网提示页都有英文版。右上角「EN / 中文」按钮随时切换，房间内切换不会打断对局。
- 语言选择保存在 Cookie `avalon_lang` 中（只保存 zh 或 en），由服务端读取，页面第一次渲染就是对应语言，不会先闪中文。第一次访问按浏览器语言决定：中文浏览器或没有语言偏好时显示中文，其他情况显示英文。
- 实现方式：以中文原文为键的英文词典（`lib/i18n/`，按页面分文件），界面文字通过 `t()` 翻译；服务端返回的报错和身份线索保持中文，由客户端 `ts()` 按原文或带占位符的模式翻译，因此规则、权限和接口完全不变。统一术语见 `lib/i18n/GLOSSARY.md`。
- 新增翻译完整性检查 `npm run i18n:check`，并纳入单元测试：界面文件中不得有未翻译的中文，所有键都要有英文，占位符一致，各文件之间不能有冲突的译文。
- 对局记录补充了统计：每次组队表决下方显示「N 赞成 · M 反对」，每次任务显示「N 张成功 · M 张失败」；导出的复盘文字同步加入成功牌张数，并支持英文导出。
- 英文界面排版：人数按钮在英文下只显示数字，品牌字距与语言切换按钮按英文调整，预设与推荐板子的英文提示精简；实测 390×844 手机和 1280×800 笔记本上，英文的人数、预设和推荐板子也在第一屏。
- 断网提示页改为中英双语，并更新了离线缓存版本，已安装的主屏幕应用会拿到新页面。

## 0.12.0 · 管理后台 · 2026-09-23

- 新增运营统计页 `/admin` 和接口 `GET /api/admin`，需要单独的管理员 Key（`ADM-XXXX-XXXX-XXXX-XXXX-XXXX`）。它和房主 Key 互不通用，服务端只保存摘要（Cloudflare Secret `ADMIN_KEY_HASHES`），未配置时拒绝访问。
- 统计内容全部是汇总数字：当前房间和在座玩家数、最近 1 小时新建、累计局次、过去 24 小时每小时新建房间（柱状图，附表格视图）、房间所处阶段、已结束对局的胜负与原因、人数和板子分布、限流情况（生效中的限流记录、因乱猜房间码被拦的网络、恢复码错误过多被锁），以及服务状态（版本、查询耗时、实时同步、房主和管理员 Key 数量）。不返回房间码、昵称、设备凭据、身份、投票或任务牌。
- Key 只能放在自定义请求头里发送，跨站页面无法附带；只接受同源请求。Key 输错按网络限流（10 分钟 10 次），接口响应 `no-store` 和 `noindex`，每次访问都写结构化日志（不记录 Key）。页面把 Key 保存在内存里，每 30 秒自动刷新，手机和桌面都能用。
- 新增 `scripts/admin-keys.mjs`，用来生成和发布管理员 Key（`--count`、`--env staging`）；本地测试 Key 为公开值 `ADM-TEST-ADMN-KEYS-2345-6789`。
- 部署后的 smoke test 会检查管理接口在没有 Key 时返回 401。隐私说明补充了运营者能看到的内容。
- 新增管理员 Key、统计汇总的单元测试，以及管理接口的集成测试（缺少或错误的 Key、房主 Key 被拒、跨站被拒、响应中不含房间码和昵称、错误次数限流）。

## 0.11.0 · 板子模板与复盘导出 · 2026-09-23

- 自定义板子新增「从模板开始」：7 个推荐板子（暗影莫德雷德、孤狼奥伯伦为 5 人起；湖中仙女经典局、兰斯洛特对决、牧师开局、迷雾揭露、失控的邪恶为 7 人起），一键套用角色和湖中仙女设置。人数不够时模板置灰并标出最少人数；是否可用直接调用服务端的板子校验判断。
- 可以把当前自定义板子保存为「我的模板」（最多 8 个，名称最多 12 字），之后一键套用或删除。没有账号，所以模板只保存在这台设备；读取时会清理无效或不可用的记录。
- 结局页新增复盘导出：「复制复盘文字」和「下载 .txt」。内容包含结果与原因、刺杀目标、全部身份、对局中公开的身份、每次任务的队员和失败牌数量，以及每次组队的队长、队员和逐人表决。不包含房间码、湖中仙女的私密查验结果，也不包含谁出了哪张任务牌（客户端本来就拿不到）。浏览器拒绝访问剪贴板时，会显示可以手动全选复制的文本框。
- 建房界面调整：默认 8 人；表单按「人数 → 角色配置 → 推荐板子 → 角色微调 → 昵称 → 房主 Key」排列，推荐板子在预设下方单独一行常驻显示，点一下即切到自定义板子并套用；手机上建房卡片排在介绍文字前面。实测 390×844、360×780 手机和 1280×800 笔记本上，人数、预设和推荐板子都在第一屏，不用往下翻。
- 调整人数时，如果当前的自定义板子在新人数下依然成立，就保留，不再每次清空；「自定义板子」改为整行显示，桌面端的间距更紧凑。
- 隐私说明补充了模板的本地存储和复盘导出。
- 新增模板校验、最少人数、本地数据清理和复盘内容的单元测试。

## 0.10.0 · 规则教学与隐私说明 · 2026-09-23

- 新增规则教学页 `/rules`：一局的五个步骤、人数与任务表（标出需要两张失败牌的任务）、预设板子、14 个角色的图鉴（阵营、能力、出现在哪些板子）、谁能看见谁、湖中仙女、胜负条件和新手建议。表格和角色信息直接取自规则引擎的数据，不会与实际规则脱节。
- 新增隐私说明页 `/privacy`：服务器保存哪些数据、谁能看到、设备上的 Cookie 与本地存储、保存期限（房间 24 小时、限流记录最长 24 小时、日志按 Cloudflare 保留期）、对局中的保密机制、基础设施提供方，以及玩家可以做什么。
- 首页页脚和使用说明弹窗加入两个页面的入口；两页适配 360px 手机宽度，宽表格在自身容器内横向滚动。
- 部署后的 smoke test 增加对这两个页面及其安全头的检查。

## 0.9.1 · 房主 Key 轮换与更易输入 · 2026-09-23

- 正式环境的房主 Key 已轮换为 1 个新 Key，此前发放的全部 Key 失效（已经建立的房间不受影响）。
- 建房的 Key 输入框改为普通文本框：只需填 16 位字母和数字，自动转为大写，自动过滤短横线、空格和容易混淆的 0/1/I/O，按 4 位一组显示，前面固定显示 `AVL-`；粘贴完整 Key（含 `AVL-`）也能识别。输满 16 位后继续输入会被忽略，退格跨过分组符时删除前一个字符，旁边显示已输入位数。
- 服务端 `verifyHostKey` 同时接受带短横线和不带短横线的写法（`AVL-XXXX-XXXX-XXXX-XXXX`、`XXXX-XXXX-XXXX-XXXX`、16 位连写，前缀可有可无，不区分大小写）；摘要仍按标准格式计算，已发布的摘要无需变化。格式不对、含易混字符或分隔符位置不对仍会被拒绝。
- `scripts/host-keys.mjs generate` 新增 `--count N`（1–50，默认 10）。
- 新增 Key 规范化、输入框过滤和分组的单元测试。

## 0.9.0 · 实时同步与 staging 环境 · 2026-09-23

- 实时同步：每个房间对应一个 Durable Object（WebSocket Hibernation API）。房间每次成功提交变更后推送“房间已到版本 N”的信号，客户端收到后通过原有带权限校验的接口读取自己的视图。实测从另一台设备操作到页面更新：本地约 0.1 秒，Cloudflare staging 约 0.4 秒（含操作请求本身）；此前轮询需要 0–3 秒。
- 信号只含版本号，不含昵称、身份、投票或任务牌，任务牌与玩家的关联仍不向客户端公开。握手要求同源 Origin 且房间存在，与读取接口共用网络限流和未知房间码查找预算；每个房间最多 48 个连接；客户端只能发送心跳，发送其他内容会被断开。
- 实时连接打开时，轮询放宽为每 30 秒一次兜底；连接不可用（旧浏览器、网络限制、房间连接已满）时自动回到 3 秒轮询。页面进入后台时断开，回到前台立即重连并补拉最新状态，断线后按指数退避重连。房间进度栏显示「实时」或「已同步」。
- D1 仍是唯一的数据源，规则、权限和并发控制不变；Durable Object 不保存任何数据。使用 SQLite 版 Durable Object，属于免费方案。
- 新增 staging 环境：独立 Worker `avalon-roundtable-staging`、独立 D1 `avalon-roundtable-staging-db`，房主 Key 白名单也与正式环境分开。新增 `npm run deploy:staging`、`npm run db:migrate:staging`、`npm run check:staging`（在 staging 上端到端验证实时信号）和 `npm run cf:tail:staging`。部署脚本会核对构建产物的 Worker 名称，防止 staging 构建覆盖正式环境。
- 部署后的 smoke test 增加实时网关检查。新增实时信号的单元测试和集成测试，覆盖握手限制、仅含版本号的广播、心跳，以及被拒绝的操作不发信号。
- 注意：v0.9 引入了 Durable Object 迁移，Cloudflare 可能拒绝把正式环境回滚到 v0.9 之前的版本；出问题时优先修复后重新部署。

## 0.8.0 · 换设备恢复与邀请安全 · 2026-09-23

- 每位玩家有一次性「换设备恢复码」（10 位，约 50 位随机性），仅本人可见；在新设备输入即可回到原座位并看到原身份，原设备立即失效，恢复码随即更换。
- 新增「请房主批准」换设备：新设备选择原座位发起请求并显示 4 位核对码，房主看到同一核对码、当面核实后批准；请求 15 分钟过期，座位变动或玩家离开后自动失效，重复批准幂等。
- 防冒用：座位原设备会收到醒目提醒并可一键拒绝；请求发出 60 秒后房主才能批准，防止房主用自己的第二台设备悄悄取得他人身份。房主座位只能用恢复码恢复，每个座位最多 3 个待处理请求。
- 每次换设备都会向全桌公开记录（座位与方式，不含身份），被替换的设备会收到提示，便于发现冒用。
- 邀请链接与二维码增加不可猜测的邀请口令（128 位）；只输入六位房间码的访客仍可入座，但看不到昵称。房主移出玩家后自动更换邀请口令。
- 未知房间码的查找（GET 与 POST）按网络限流（每 10 分钟 30 次）；超限后该网络的陌生设备被拒绝，已入座玩家不受影响，避免同一 Wi-Fi 下的朋友被连累。恢复码只统计错误尝试，按座位与网络限流，垃圾请求无法锁死本人恢复。房间过期或关闭后客户端停止轮询。
- 房间状态加入 `schemaVersion: 2`；v0.8 之前建立的房间保持原行为直到 24 小时过期，无需数据库迁移。
- 新增恢复码、邀请口令、房主批准、原设备拒绝、等待窗口、网络限流的单元测试和真实接口集成测试，以及 360px 手机界面走查；并经过独立安全审查后修复发现的问题。

## 0.7.0 · 企业级基础治理 · 2026-09-23

- 全仓 ESLint 清零（`npm run lint` 以 0 警告为准）：修复 Effect 内同步更新状态、Hook 依赖、内部链接、二维码图片与测试未用变量；生成目录不再被扫描。
- 自定义 Worker 入口为所有页面和接口统一添加 CSP（含 `frame-ancestors 'none'`）、HSTS、Referrer-Policy、Permissions-Policy、X-Frame-Options、COOP/CORP；静态文件通过 `public/_headers` 使用同一策略，并有测试保证两者一致。
- 接口请求生成 request ID，返回 `X-Request-Id`，按 JSON 结构化日志记录动作、匿名房间摘要、状态码、耗时与并发冲突次数；5xx 错误提示附带错误编号。
- 新增 `/api/health` 健康检查（版本、数据库连通性），以及每小时 Cloudflare Cron 分批清理过期房间与限流记录。
- 部署后自动运行只读 smoke test；新增 `cf:deployments`、`cf:rollback`、`cf:tail` 命令。
- CI 增加 lint、生产依赖审计（阻断）、全量依赖审计（报告），以及在本地 D1 上启动服务并运行全部多人集成测试。
- 依赖安全：React 19.2.8、Vite 8.0.16、Vinext beta.11、Wrangler 4.136、Cloudflare Vite 插件 1.57；`npm audit` 为 0。移除 56 个未使用的 UI 组件与 15 个未使用依赖。

## 0.6.0 · 扩展角色与自定义板子 · 2026-09-23

- 建房新增自定义板子，按人数实时显示正义／邪恶名额，空位自动补为忠臣或爪牙。
- 新增正义兰斯洛特、邪恶兰斯洛特、牧师、疯子、野蛮人和揭露者，并完整实现私密线索、强制任务牌与公开揭露规则。
- 新增湖中仙女可选模块：第 2、3、4 次任务后由持有者私密查验阵营，结果只投影给查验者，令牌随后转交目标。
- 服务端校验自定义阵营数量、唯一角色、梅林／刺客必选、莫甘娜与派西维尔依赖、兰斯洛特成对及 7 人扩展下限。
- 房间大厅公开展示最终阵容和湖中仙女开关；同房重开保留自定义板子并重新洗牌。
- 新增扩展规则、任务牌约束、公开／私密投影与湖中仙女幂等性测试。

## 0.5.0 · 产品界面与房主 Key · 2026-09-16

- 重设计首页、建房与入房表单、圆桌座位、私密身份、邀请二维码及说明弹窗，统一深墨绿与暖金视觉。
- 新增公开的四阶段进度轨道，突出准备与身份确认进度。
- 重做对局比分、任务总览、玩家选取、投票卡、私密任务票、结果与复盘记录。
- 手机将当前操作提前；房间管理收纳于底部，增加清晰焦点、大尺寸按钮和长昵称适配。
- 连接恢复期间显示加载状态，进入或离开房间自动回到页面顶部；身份卡保持固定高度，防止按住查看时位移。
- 房主凭有效 Key 建房，服务器校验 SHA-256 摘要；缺失、错误或未配置时拒绝创建，朋友加入及同房重开不需 Key。
- 10 个随机 Key 可重复使用，仅写入本地忽略文件，Cloudflare Secret 保存允许的摘要；提供生成与发布脚本。
- 建房输入中的 Key 不保存至浏览器存储、房间数据库或 GitHub，成功后清除；错误提示在表单内显示。
- 保留全部服务端规则、权限与任务牌隐私；不增加收费服务或外部字体依赖。
- 类型检查排除本地临时工作目录与构建产物，避免未提交的测试草稿干扰检查。

## 0.4.0 · MVP 房间管理 · 2026-09-16

- 房主可在发身份前移出玩家、空出座位，其他人不受影响。
- 支持主动移交房主，保留原有座位、身份和对局进度。
- 进行中的对局可由房主确认作废，不判胜负、不揭晓身份，清空记录后回到准备大厅。
- 房主变更及局次共同隔离管理请求，防止旧房主操作重放、重复中止及误移出同座位新玩家。
- 增加被移出和上一局作废的提示，管理状态改变时关闭过期确认窗口。
- 增加权限、隐私与并发管理测试。

## 0.3.0 · 第三阶段 · 2026-09-16

- 房主可在结局后同房再开，保留玩家与座位，清空上一局身份、投票与进度，重新准备发身份。
- 按局隔离请求，防止上一局延迟操作影响新局；并发重开只推进一局。
- 加入主屏幕安装清单、手机图标及 iPhone / Android 安装说明。
- 仅缓存公开断网提示页，私密身份、房间状态及投票不进入离线缓存，不在后台补发操作。
- 断网立即停止提交，联网、返回前台时同步；新局自动收起旧身份与确认窗口。
- 补充同房第二局、重开权限、跨局旧请求和离线缓存隐私测试。
- 真机多人聚会验收仍待实际 iPhone / Android 设备确认。

## 0.2.0 · 第二阶段 · 2026-09-16

- 全员确认后开始完整对局，队长按座位轮换选人。
- 组队票收齐后逐人公开，支持平票否决和连续五次否决判负。
- 队员私密提交任务牌，支持第四任务双失败规则。
- 三次任务成功后由刺客选择目标，自动判定最终胜负。
- 查看逐轮队伍、表决记录和任务结果；结束后成员可查看全部角色。
- 防重复末票、防跨轮重放，保持任务票与玩家的关联私密。
- 23 项规则单测及完整多人接口流程验证。

## 0.1.0 · 第一阶段 · 2026-09-16

- 建房、扫码入座、准备、私密身份与线索确认。
- 独立部署到个人 Cloudflare Workers + D1。
- 支持从完整源码克隆、安装、初始化本地数据库和继续开发。
