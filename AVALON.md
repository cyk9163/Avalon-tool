# 圆桌 · 阿瓦隆助手

第一阶段：5–10 人建房、房间码/二维码邀请、昵称入座、准备、服务端发身份、私密角色线索、全员确认、同浏览器刷新恢复。

第二阶段：队长选队、全员组队票、匿名任务牌、五次否决判负、任务胜负、刺杀、结束后角色揭晓，以及整局公开表决记录。旧版本已确认身份的房间可以由房主直接开始对局。

第三阶段：同房再开、主屏幕安装入口、断网提示、恢复网络和前台返回同步。代码与自动化验证完成后发布；真实 iPhone / Android 多人聚会验收仍需实际设备确认。

MVP 收尾（v0.4）：房主可在发身份前移出玩家，随时移交房主，或中止正在进行的对局、回到大厅调整人员后重开。

v0.7：企业级基础治理——lint 清零、安全响应头、结构化日志与 request ID、健康检查、Cron 定时清理、依赖漏洞清零、CI 覆盖 lint／审计／集成测试、部署后 smoke test。

v0.8：换设备恢复（一次性恢复码、房主当面核实后批准）、公开换设备记录、带不可猜口令的邀请链接、未知房间码查找限流。

v1.0：规则教学页、隐私说明页、板子模板、复盘导出、管理后台（只读汇总统计）和中英双语。

v0.9：实时同步——每个房间一个 Durable Object 通过 WebSocket 推送变更信号（只含版本号），连接可用时轮询降为 30 秒兜底；新增独立的 staging 环境（Worker、D1、房主 Key 均与正式环境分开）。

v0.5：重做手机与桌面视觉、阶段进度、选队／投票／复盘界面；房主凭服务器验证的 Key 建房，朋友入房不需要 Key。完整交接见 `HANDOFF.md`。

## 实现

- React + TypeScript 手机界面，运行在 Cloudflare Workers 兼容的 Vinext。
- 使用个人 Cloudflare 账户中的 D1 持久化。v0.9 起每个房间有一个 Durable Object，通过 WebSocket 推送“房间已到版本 N”，客户端再经授权接口读取自己的视图；实时连接不可用时回到每 3 秒轮询，后台页面暂停同步。
- 一个房间保存为一条带版本号的记录，通过条件更新、重试防止并发抢座和重复发牌。
- HttpOnly / SameSite=Strict Cookie 保存设备凭据；数据库只保存其 SHA-256 摘要。生产 HTTPS Cookie 使用 Secure。
- 房间公开响应仅含座位和准备状态；对局详情与私密身份按服务器认证的设备凭据投影。游戏结束前房主没有特殊身份读取权限，结束后本局成员可查看所有角色。
- 组队票收齐前只显示谁已提交，收齐后逐人公开。任务牌只公布总数，直到游戏结束也不公开个人任务牌；后台或失焦会关闭私密任务牌窗口。
- 服务器执行全部阶段、队长、任务队员、阵营与刺客权限校验；每轮有独立编号，旧请求重试不能投进新一轮，重复最后一票不会重复推进。
- 结束后仅房主可以同房再开：保留房间、玩家和座位，清空身份及记录，重新准备。每局有独立编号；跨局旧请求不能发身份、确认、投票、换座或退出新局，重复重开不会重复清空。
- 房间管理按当前房主、局次与房主权限版本校验。转让保留游戏状态；移出仅限大厅并按玩家 ID 定位，重试不会误移除同座位的新玩家。移出不是封禁。
- 中止对局不判定胜负或揭晓身份，清除私密状态与记录并进入下一局大厅；旧请求不能操作新局。原房主移交后没有管理权，房主轮转后的旧请求也不能重放。
- PWA 的 Service Worker 只缓存公开断网提示页，不缓存 API、身份、房间页面或投票，也不排队补发操作。安装前后可能使用不同浏览器会话，建议先安装再入房。
- 房间创建 24 小时后过期；每小时 Cron 与新建房间时都会分批清理过期数据。房间不用于长期战绩存储。
- 同设备同浏览器的多个标签页代表同一个玩家。清除 Cookie 或换设备后，用本人的一次性恢复码或房主批准回到座位，原设备立即失效；房主座位只接受恢复码。
- 页面身份默认隐藏；切后台/失焦重新遮罩。二维码包含房间码和邀请口令（用于查看昵称），不包含玩家凭据。

## 本地开发

要求 Node 24。依次运行 `npm ci`，复制 `.dev.vars.example` 为 `.dev.vars`（保留已有私有配置），再运行 `npm run db:migrate:local`、`npm run dev`。本地房主 Key 使用 README 中的公开测试值，正式环境使用 Cloudflare Secret `HOST_KEY_HASHES`。本地开发不需要登录任何云服务。

本机 npm 的命令脚本有路径解析问题时，直接运行 `node scripts/run-framework.mjs dev` 或 `node scripts/run-framework.mjs build`。

数据库变更使用 `npm run db:generate`，然后 `npm run db:migrate:local`。线上迁移使用 `npm run db:migrate:remote`，部署使用 `npm run deploy`，两者需要 Cloudflare 登录。

## 验证

```sh
npm run lint
npm run typecheck
npm test
npm run test:integration
```

集成测试默认使用 `http://localhost:5173`，创建会自动过期的测试房间，覆盖 5/7/10 人并发入座、准备、双发牌、确认、越权访问、CSRF 和会话恢复。只在专用测试环境运行。

## 经典规则

严格过半才批准队伍，平票否决；连续第五次否决直接邪恶胜。批准队伍后否决计数归零。5/6/7/8–10 人的五次任务人数分别为 2-3-2-3-3、2-3-4-3-4、2-3-3-4-4、3-4-4-5-5。只有 7 人及以上的第 4 任务需要两张失败牌。

三次任务失败直接邪恶胜；三次成功后进入刺杀。刺客选择怀疑是梅林的另一个座位，一次锁定结算：梅林被选中则邪恶胜，否则好人胜。服务器不会用可重试的错误响应告诉刺客目标的隐藏阵营。

规则依据：[发行商验证的阿瓦隆规则](https://rules.dized.com/game/rZluqS52QmGdpoVxcmVLtg/the-resistance-avalon)。

## 后续方向

目前尚未提供永久战绩及导出。房间仍在创建 24 小时后过期。下一步是实际 iPhone / Android 聚会验收；v0.9（Durable Objects + WebSocket、staging 环境）与 v1.0（板子模板、规则教学、复盘导出、中英双语、管理后台、隐私页面）均已完成。下一步仍是真机聚会验收；可以考虑的后续方向有更多语言、高级角色模块（信使、游侠、术士、不可信任的仆人）和兰斯洛特阵营转换变体。当前全部使用免费方案，无需付费升级。
